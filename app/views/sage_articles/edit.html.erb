<%= title l(:"facturaplus.label_edit_sage_article") %>

<%#= labelled_form_for @relation, :url => synch_relations_path do |f| %>
<%= labelled_form_for @sage_article, :html => {:id => 'issue-form', :multipart => true} do |f| %>
  <%= error_messages_for 'sage_articles' %>
  <div class="box tabular">
    <div id="all_attributes">
        <%= render :partial => 'sage_articles/form', :locals => {:f => f} %>
    </div>

  </div>

  <%= submit_tag l(:button_update) %>
<% end %>

<%= link_to l(:button_back), sage_articles_path %>
